'use client';

/**
 * Navigation component — no longer used in layout.tsx (replaced by Sidebar).
 * Kept for backward compatibility. Renders nothing.
 */
export function Navigation() {
  return null;
}
